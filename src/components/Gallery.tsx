import React, { useEffect, useState } from 'react';
import { BusinessConfig, GalleryItem } from '../types/business';
import { X, ZoomIn } from 'lucide-react';
interface GalleryProps{business:BusinessConfig}
export const Gallery:React.FC<GalleryProps>=({business})=>{const[selectedCategory,setSelectedCategory]=useState('Semua');const[activeModalImage,setActiveModalImage]=useState<GalleryItem|null>(null);const[visible,setVisible]=useState(true);if(!business.gallery?.length)return null;const categories=['Semua',...Array.from(new Set(business.gallery.map(g=>g.category)))];const filtered=selectedCategory==='Semua'?business.gallery:business.gallery.filter(g=>g.category===selectedCategory);useEffect(()=>{setVisible(false);const id=window.setTimeout(()=>setVisible(true),70);return()=>window.clearTimeout(id)},[selectedCategory]);return <section id="galeri" className="scroll-mt-20 bg-white py-16 sm:py-20"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="mx-auto mb-10 max-w-3xl space-y-3 text-center"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold uppercase tracking-wider" style={{color:business.theme.primaryColor}}>Dokumentasi</span><h2 className="text-2xl font-extrabold tracking-tight text-slate-900 sm:text-3xl lg:text-4xl">Lihat Aktivitas & Hasil Pekerjaan Kami</h2><p className="text-sm text-slate-600 sm:text-base">Pilih kategori untuk melihat dokumentasi yang paling relevan dengan kebutuhan Anda.</p></div>{categories.length>2&&<div className="mb-10 flex flex-wrap items-center justify-center gap-2">{categories.map(cat=><button key={cat} type="button" onClick={()=>setSelectedCategory(cat)} className={`rounded-xl px-4 py-2 text-xs font-semibold transition-all duration-300 sm:text-sm ${selectedCategory===cat?'text-white shadow-md scale-105':'bg-slate-100 text-slate-600 hover:-translate-y-0.5 hover:bg-slate-200'}`} style={{backgroundColor:selectedCategory===cat?business.theme.primaryColor:undefined}}>{cat}</button>)}</div>}<div className={`grid grid-cols-1 gap-6 transition-all duration-500 sm:grid-cols-2 lg:grid-cols-3 ${visible?'translate-y-0 opacity-100':'translate-y-3 opacity-0'}`}>{filtered.map((item,i)=><div key={item.id} onClick={()=>setActiveModalImage(item)} className="group relative aspect-[4/3] cursor-pointer overflow-hidden rounded-2xl border border-slate-200 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-xl" style={{transitionDelay:`${i*45}ms`}}><img src={item.imageUrl} alt={item.title} className="h-full w-full object-cover transition-transform duration-700 group-hover:scale-105" loading="lazy"/><div className="absolute inset-0 flex flex-col justify-end bg-gradient-to-t from-slate-950/80 via-slate-950/20 to-transparent p-5 text-white opacity-0 transition-opacity duration-300 group-hover:opacity-100"><span className="mb-1 text-[11px] font-bold uppercase tracking-wider text-amber-400">{item.category}</span><h4 className="text-base font-bold leading-snug">{item.title}</h4>{item.description&&<p className="mt-1 line-clamp-2 text-xs text-slate-300">{item.description}</p>}<div className="mt-3 flex items-center gap-1.5 text-xs font-semibold"><ZoomIn className="h-4 w-4"/>Klik untuk perbesar</div></div></div>)}</div>{activeModalImage&&<div className="fixed inset-0 z-[120] flex items-center justify-center bg-slate-950/80 p-4 backdrop-blur-sm"><button type="button" aria-label="Tutup" onClick={()=>setActiveModalImage(null)} className="absolute right-5 top-5 z-20 rounded-full bg-slate-900/70 p-2 text-white hover:bg-slate-900"><X className="h-5 w-5"/></button><div className="absolute inset-0" onClick={()=>setActiveModalImage(null)}/><div className="relative z-10 w-full max-w-3xl overflow-hidden rounded-2xl bg-white shadow-2xl animate-gallery-modal"><div className="aspect-[16/10] bg-black"><img src={activeModalImage.imageUrl} alt={activeModalImage.title} className="h-full w-full object-cover"/></div><div className="p-5"><span className="text-xs font-bold uppercase tracking-wider" style={{color:business.theme.primaryColor}}>{activeModalImage.category}</span><h3 className="mt-1 text-lg font-bold text-slate-900">{activeModalImage.title}</h3>{activeModalImage.description&&<p className="mt-1.5 text-sm text-slate-600">{activeModalImage.description}</p>}</div></div></div>}</div></section>};
